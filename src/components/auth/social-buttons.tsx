"use client";
import { signIn } from "next-auth/react";
import { useState } from "react";
export function SocialButtons({ callbackUrl = "/dashboard" }: { callbackUrl?: string }) { const [error, setError] = useState(""); const login = async (provider: "google" | "facebook") => { setError(""); const result = await signIn(provider, { callbackUrl }); if (result?.error) setError("Social sign-in is not configured or unavailable."); }; return <div className="mt-6 space-y-3"><div className="flex items-center gap-3 text-xs text-forest/50"><span className="h-px flex-1 bg-forest/15" />or continue with<span className="h-px flex-1 bg-forest/15" /></div><div className="grid gap-3 sm:grid-cols-2"><button type="button" onClick={() => login("google")} className="rounded-full border border-forest/20 bg-white px-4 py-3 text-sm font-semibold">Continue with Google</button><button type="button" onClick={() => login("facebook")} className="rounded-full border border-forest/20 bg-white px-4 py-3 text-sm font-semibold">Continue with Facebook</button></div>{error && <p role="alert" className="text-sm text-red-800">{error}</p>}</div>; }
